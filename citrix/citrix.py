from talon import Context, Module, actions, settings, ctrl

module = Module()
context = Context()

module.tag("citrix", "Tag for enabling commands inside citrix")

# module.mode("vdi_vim", "vim on vdi")
